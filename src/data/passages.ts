/**
 * Superseded by `src/data/index.ts`, which serves structured, cited passages
 * from `bundles/`. Re-exported here so existing imports keep working.
 */
export { COUNTY_PASSAGES } from './index';
